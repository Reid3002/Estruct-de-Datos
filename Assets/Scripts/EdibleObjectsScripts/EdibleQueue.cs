using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EdibleQueue : MonoBehaviour
{
    [Header("Items and positions")]
    public List<EdibleController> edibleItemsList = new List<EdibleController>();
    public List<Vector2> edibleItemsPosition = new List<Vector2>();
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
