using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test001 : MonoBehaviour
{
    public GridHashingV2 gridScript;
    // Start is called before the first frame update
    void Start()
    {
        for (int i = 0; i < 50; i++)
        {

        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
